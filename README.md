``` bash

$ git clone https://github.com/linghart/ql-laravel.git

$ cd ql-laravel
$ composer install

$ cp .env.example .env
$ php artisan migrate
$ php artisan seed

$ php artisan jwt:secret

$ php artisan serve

```
